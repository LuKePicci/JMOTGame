package it.polimi.ingsw.cg_30.exchange.messaging;

/**
 * The Enum ActionType.
 */
public enum ActionType {

    /** The move action. */
    MOVE,
    /** The attack action. */
    ATTACK,
    /** The turn over action. */
    TURN_OVER,
    /** The use item card action. */
    USE_ITEM,
    /** The noise any action. */
    NOISE_ANY,
    /** The discard card action. */
    DISCARD_CARD,
    /** The draw card action. */
    DRAW_CARD;
}
